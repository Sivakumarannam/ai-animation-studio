Single pass, please — fix everything below together, don't re-test things
that already passed before (322 backend tests were green last run; only
re-run the suite once at the very end, not after each individual fix).

FIX 1 — Same dispatch bug, second location
backend/apps/api/routers/asset_generation.py line 573 (retry_entry endpoint)
has the identical bug already fixed once at line 514:
  mode = await dispatcher.dispatch(task=..., ...)
  → TypeError: TaskDispatcher.dispatch() got an unexpected keyword argument 'task'
  → RuntimeWarning: coroutine '_process_retry_queue_core' was never awaited
Fix it the same way line 514 was fixed.

Then run this ONE check across the whole routers directory to catch any
other instance of the same wrong pattern before it surfaces later:
  grep -rn "dispatcher.dispatch(" backend/apps/api/routers/
Fix any other call site using the wrong keyword arguments. Do not just
report them — fix them now, in this same pass.

STATUS CHECK 1 — file_size_bytes gap
You flagged this as outstanding in the last report. Tell me directly:
is it fixed or not, and does it affect anything a user would actually see
or click on (e.g. missing file size shown as 0/blank in Asset Library)?
If it's a real user-visible gap, fix it now in this same pass. If it's
internal/cosmetic and doesn't affect usability, just confirm that and leave
it — don't spend time on a cosmetic-only fix right now.

STATUS CHECK 2 — MinIO image upload / Asset Library rendering
Confirm explicitly, don't assume: does the image generated by a completed
job actually get uploaded to MinIO and render as a visible image in Asset
Library right now? Check this directly (query the DB record / storage path
for the most recent completed asset, or check the relevant service code)
rather than assuming from the earlier fix. If it's NOT actually happening,
fix it now in this same pass, using the existing MinIO upload pattern from
asset_manager.py or library_service.py.

AFTER ALL FIXES ABOVE:
1. Restart backend + Celery.
2. Run the full backend test suite once (not per-fix) — confirm it's still
   all green.
3. I will personally test the following three things in the browser myself
   — do not spend credits re-verifying these, just make sure the code is
   correct and stop there:
   - Clicking Retry on a retry-queue entry
   - Whether a generated image visibly renders in Asset Library
   - file_size_bytes showing a real value (if you fixed it)
4. Update PHASE6_COMPLETION_REPORT.md with a short summary of exactly what
   was fixed in this pass.

Do not add new features, do not refactor unrelated code, do not re-run
tests multiple times — one fix pass, one test run, one report update.